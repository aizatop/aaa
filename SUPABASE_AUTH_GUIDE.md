# 🔐 Инструкция по настройке аутентификации Supabase

## 📋 Что нужно сделать:

### 1️⃣ Включение аутентификации в Supabase

1. **Перейдите в ваш проект Supabase**
2. **В меню выберите "Authentication"**
3. **Убедитесь что аутентификация включена** (обычно включена по умолчанию)

### 2️⃣ Настройка провайдеров входа

1. **В "Authentication" → "Providers"**
2. **Включите "Email" провайдер:**
   - ✅ Enable Email provider
   - ✅ Allow new users to sign up
   - ✅ Confirm email (опционально)

### 3️⃣ Создание таблицы профилей пользователей

1. **Перейдите в "Table Editor"**
2. **Создайте новую таблицу:**
   ```
   Name: profiles
   Description: Профили пользователей
   ```

3. **Добавьте колонки:**
   ```sql
   - id (uuid, primary key, default: uuid_generate_v4())
   - user_id (uuid, foreign key to auth.users.id, unique)
   - username (text, not null)
   - email (text, not null)
   - avatar_url (text, optional)
   - created_at (timestamp, default: now())
   - updated_at (timestamp, default: now())
   ```

4. **Настройте foreign key:**
   - `user_id` → `auth.users.id`
   - Включите "Cascade delete"

### 4️⃣ Настройка RLS для таблицы profiles

1. **Перейдите в "Authentication" → "Policies"**
2. **Выберите таблицу "profiles"**
3. **Создайте политику для просмотра:**
   ```
   Policy name: Users can view own profile
   Allowed operation: SELECT
   Policy definition: user_id = auth.uid()
   ```
4. **Создайте политику для вставки:**
   ```
   Policy name: Users can insert own profile
   Allowed operation: INSERT
   Policy definition: user_id = auth.uid()
   ```
5. **Создайте политику для обновления:**
   ```
   Policy name: Users can update own profile
   Allowed operation: UPDATE
   Policy definition: user_id = auth.uid()
   ```

### 5️⃣ Настройка URL для редиректа

1. **Перейдите в "Project Settings" → "Authentication"**
2. **В "Site URL" добавьте:**
   ```
   http://localhost:3000  (для разработки)
   https://yourdomain.com (для продакшена)
   ```
3. **В "Redirect URLs" добавьте те же URL**

### 6️⃣ Обновление конфигурации

1. **Откройте файл `supabase-config.js`**
2. **Замените placeholder значения:**
   ```javascript
   const supabase = createClient(
       'https://your-project-id.supabase.co',    // Ваш Project URL
       'your-anon-public-key-here'             // Ваш anon public key
   );
   ```

## 🚀 Что будет работать после настройки:

### ✅ Функционал аутентификации:
- **Регистрация** через email и пароль
- **Вход** в существующий аккаунт
- **Выход** из аккаунта
- **Сохранение** сессии между перезагрузками
- **Автоматическое** обновление UI при входе/выходе

### 🎯 Преимущества Supabase Auth:
- **Безопасное** хранение паролей
- **Email подтверждение** (опционально)
- **Восстановление** пароля
- **JWT токены** для безопасности
- **Realtime** сессии

## 📱 Тестирование:

1. **Зарегистрируйте нового пользователя**
2. **Проверьте email** (если включено подтверждение)
3. **Войдите в аккаунт**
4. **Обновите страницу** - сессия должна сохраниться
5. **Проверьте таблицу profiles** - должен создаться профиль

## 🔒 Безопасность:

- **RLS политики** защищают данные пользователей
- **JWT токены** автоматически управляются
- **Пароли** надежно хешируются
- **Сессии** безопасно хранятся

---

**Готово к использованию! 🔐**
